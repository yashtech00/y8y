import express from "express";

const formRoutes = express.Router();

// formRoutes.post("/create", formController.createsForms);
// formRoutes.get("/get-forms", formController.getForms);


export default formRoutes;